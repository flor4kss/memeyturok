services:
  - type: web
    name: telegram-meme-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m bot.main"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 8080
